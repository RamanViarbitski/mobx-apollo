{
  "name": "mobx-apollo",
  "description": "A MobX and Apollo Client integration utility.",
  "version": "0.0.18",
  "main": "./lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sonaye/mobx-apollo.git"
  },
  "peerDependencies": {
    "mobx": "^3.2.2"
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-preset-es2015": "6.24.1",
    "babel-preset-stage-0": "6.24.1",
    "cross-env": "5.1.1"
  },
  "scripts": {
    "build":
      "mkdir -p lib && cross-env NODE_ENV=production babel src --out-file lib/index.js --presets=es2015,stage-0 --watch"
  }
}
